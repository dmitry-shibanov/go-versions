name: Create Pull Request
on:
  workflow_dispatch:

jobs:
  create-pr:
    uses: dmitry-shibanov/versions-package-tools/.github/workflows/build-tool-packages.yml@v-dmshib/add-hash-file
    with:
      tool-name: "go"
    secrets: inherit